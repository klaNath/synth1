
module synth1_test();
  
  `timescale 1ns / 1ns
  
  reg         clk;

  parameter CLOCK = 10;
  
  initial clk <= 0;
  
  always #(CLOCK / 2) clk = ~clk;  
  
synth1 s1(
        .clk(clk),
        .sck(),
        .ss_n(),
        .lrck(),
        .sdo(),
        .bck());
        
endmodule